::::: {.content-visible when-meta="curate"}

## Curation

```{python}
#| output: asis
if curate and glob.glob(f"{log_path}/*-curated-*.assembly_summary"):
    displayGfastatsAssemblySummary(f"{log_path}/*-curated-*.assembly_summary")
```

```{python}
#| output: asis
if curate and glob.glob(f"{log_path}/*-curated-*_merquryfk.completeness.stats"):
    displayAssemblyCompleteness(f"{log_path}/*-curated-*_merquryfk.completeness.stats")
    displayAssemblyQualityValue(f"{log_path}/*-curated-*_merquryfk.qv")
    if(glob.glob(f"{log_path}/*-curated-*_merqury.*.qv")):
        displayScaffoldQualityValue(f"{log_path}/*-curated-*_merqury.*.qv")
```

```{python}
#| output: asis
if curate and (
    glob.glob(f"{log_path}/*-curated-*_merquryfk*.spectra-cn.st.png")
    or glob.glob(f"{log_path}/*-curated-*_merqury.*.spectra-cn.st.png")
):
    displayMerquryCopyNumberSpectra(
        f"{log_path}/*-curated-*_merquryfk*.spectra-cn.st.png",
        f"{log_path}/*-curated-*_merqury.*.spectra-cn.st.png"
    )

if curate and (
    glob.glob(f"{log_path}/*-curated-*_merquryfk.spectra-asm.st.png")
    or glob.glob(f"{log_path}/*-curated-*_merqury.spectra-asm.st.png")
):
    displayMerquryAssemblySpectra(
        f"{log_path}/*-curated-*_merquryfk.spectra-asm.st.png",
        f"{log_path}/*-curated-*_merqury.spectra-asm.st.png"
    )

if curate and glob.glob(f"{log_path}/*-curated-*_merquryfk.false_duplications.tsv"):
    displayMerquryFalseDuplicationEstimate(f"{log_path}/*-curated-*_merquryfk.false_duplications.tsv")
```

:::::
